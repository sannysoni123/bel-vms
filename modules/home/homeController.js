'use strict';

    var homeModule = angular.module('homeModule.controllers', []);

    homeModule.controller('homeCtrl', function($window,$scope,additionService){
 
/*	additionService.getLoginService(username,password)
	 .then(function(data) {
       alert(data);
        }).catch(function(error){
            $log.error('ERROR:', error);
            throw error;
        });*/
        
    });